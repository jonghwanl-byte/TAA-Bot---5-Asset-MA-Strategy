name: Daily MA Strategy Report

on:
  schedule:
    # 매일 23:00 UTC에 실행 (한국 시간 KST = UTC + 9, 즉 오전 8시)
    - cron: '0 23 * * *'
    
  workflow_dispatch: # Actions 탭에 수동 실행 버튼을 표시하는 트리거
    inputs:
      test_mode:
        description: 'Enable Test Mode (Bypasses weekend check, runs on latest data).'
        required: true
        default: 'FALSE'
        type: choice
        options:
        - 'TRUE'
        - 'FALSE'

jobs:
  run_daily_strategy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📦 코드 체크아웃
      uses: actions/checkout@v3

    - name: 🐍 Python 환경 설정
      uses: actions/setup-python@v3
      with:
        python-version: '3.x'

    - name: ⚙️ 종속성 설치 (requirements.txt)
      # Pip 경로 문제를 해결하기 위해 upgrade와 install을 명시적으로 실행
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    # --- 1. MA 전략 실행 및 결과 캡처 (최종 환경 PATH 설정) ---
    - name: 🚀 MA 전략 실행 및 결과 캡처
      id: strategy_output
      env:
        TEST_MODE: ${{ github.event.inputs.test_mode }}
      run: |
        # 🚨 PYTHONPATH 설정: 설치된 라이브러리 경로를 Python이 인식하도록 강제합니다.
        PYTHON_PACKAGES=$(python -c "import site; print(site.getsitepackages()[0] if site.getsitepackages() else '')")
        export PYTHONPATH=$PYTHON_PACKAGES:$PYTHONPATH
        export TEST_MODE=${{ github.event.inputs.test_mode }}

        # 스크립트 실행 (STDERR를 STDOUT으로 리다이렉션하여 오류 캡처)
        REPORT_CONTENT=$(python daily_signal_generator.py 2>&1)
        
        # GITHUB_OUTPUT에 캡처된 내용 할당
        echo "report<<EOF" >> $GITHUB_OUTPUT
        echo "$REPORT_CONTENT" >> $GITHUB_OUTPUT
        echo "EOF" >> $GITHUB_OUTPUT

    # --- 2. 텔레그램 메시지 전송 ---
    - name: 📩 텔레그램 메시지 전송
      uses: appleboy/telegram-action@master
      with:
        to: ${{ secrets.TELEGRAM_CHAT_ID }} 
        token: ${{ secrets.TELEGRAM_TOKEN }}
        message: ${{ steps.strategy_output.outputs.report }}
        format: markdown 
      if: always()
